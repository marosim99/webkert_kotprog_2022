import { EventDataPipe } from './event-data.pipe';

describe('EventDataPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDataPipe();
    expect(pipe).toBeTruthy();
  });
});
